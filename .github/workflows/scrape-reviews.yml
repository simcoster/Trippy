name: Scrape reviews

# Daily Google reviews scrape. SSH and report live in scrape-job.yml.

on:
  workflow_dispatch:
    inputs:
      extra_args:
        description: Extra args, e.g. --campsite-id 2
        required: false
        default: ""
  schedule:
    # 09:00 IDT (Israel summer). Winter (IST) this is 08:00.
    - cron: "0 6 * * *"

concurrency:
  group: scrape
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  scrape:
    uses: ./.github/workflows/scrape-job.yml
    with:
      job: reviews
      extra_args: ${{ github.event.inputs.extra_args || '' }}
      report_file: reviews.md
    secrets: inherit
