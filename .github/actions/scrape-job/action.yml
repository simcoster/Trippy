name: Scrape on VM
description: SSH to the Nebius VM and run job.sh. Not a workflow; callers check this out.

inputs:
  job:
    description: job.sh name (availability, reviews, prices, …)
    required: true
  extra_args:
    description: Extra args, e.g. --site 2
    required: false
    default: ""
  report_file:
    description: Markdown under ~/.trippy-scrape to copy into the Summary
    required: false
    default: ""
  vm_host:
    required: true
  ssh_user:
    required: true
  ssh_key:
    required: true
  trippy_root:
    required: false
    default: /opt/trippy

runs:
  using: composite
  steps:
    - name: Run scrape on the VM
      uses: appleboy/ssh-action@v1.2.2
      env:
        JOB: ${{ inputs.job }}
        EXTRA: ${{ inputs.extra_args }}
        TRIPPY_ROOT: ${{ inputs.trippy_root }}
      with:
        host: ${{ inputs.vm_host }}
        username: ${{ inputs.ssh_user }}
        key: ${{ inputs.ssh_key }}
        command_timeout: 180m
        envs: JOB,EXTRA,TRIPPY_ROOT
        script: |
          set -eu
          cd "${TRIPPY_ROOT}"
          REPORT_DIR="${HOME}/.trippy-scrape"
          mkdir -p "${REPORT_DIR}"
          rm -f "${REPORT_DIR}/availability.md" "${REPORT_DIR}/reviews.md" \
            "${REPORT_DIR}/prices.md"
          if [ -n "${EXTRA}" ]; then
            docker compose -f docker-compose.prod.yml --env-file .env \
              --profile scrape run --rm \
              -v "${REPORT_DIR}:/reports" \
              -e AVAILABILITY_REPORT_PATH=/reports/availability.md \
              -e REVIEWS_REPORT_PATH=/reports/reviews.md \
              -e PRICES_REPORT_DIR=/reports \
              -e PRICES_REPORT_PATH=/reports/prices.md \
              scrape "${JOB}" ${EXTRA}
          else
            docker compose -f docker-compose.prod.yml --env-file .env \
              --profile scrape run --rm \
              -v "${REPORT_DIR}:/reports" \
              -e AVAILABILITY_REPORT_PATH=/reports/availability.md \
              -e REVIEWS_REPORT_PATH=/reports/reviews.md \
              -e PRICES_REPORT_DIR=/reports \
              -e PRICES_REPORT_PATH=/reports/prices.md \
              scrape "${JOB}"
          fi
          docker compose -f docker-compose.prod.yml --env-file .env \
            --profile load run --rm price-sandbox-loader

    - name: Job summary
      if: always()
      env:
        HOST: ${{ inputs.vm_host }}
        SSH_USER: ${{ inputs.ssh_user }}
        SSH_KEY: ${{ inputs.ssh_key }}
        REPORT_FILE: ${{ inputs.report_file }}
      shell: bash
      run: |
        set -eu
        if [ -z "${REPORT_FILE}" ]; then
          echo "_No scrape report for this job._" >> "$GITHUB_STEP_SUMMARY"
          exit 0
        fi
        key="$(mktemp)"
        trap 'rm -f "$key"' EXIT
        printf '%s\n' "$SSH_KEY" > "$key"
        chmod 600 "$key"
        ssh -i "$key" -o StrictHostKeyChecking=accept-new \
          -o UserKnownHostsFile=/dev/null \
          "${SSH_USER}@${HOST}" \
          "test -f \"\$HOME/.trippy-scrape/${REPORT_FILE}\" && cat \"\$HOME/.trippy-scrape/${REPORT_FILE}\" || echo \"_No scrape report (the scrape failed before writing one)._\"" \
          >> "$GITHUB_STEP_SUMMARY"
